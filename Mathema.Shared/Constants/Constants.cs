using System;

namespace Mathema.Shared.Constants
{
    public class Constants
    {
        public static decimal PI { get; } = (decimal)Math.PI;

        public static decimal e { get; } = (decimal)Math.E;

        public static decimal EM { get; } = 0.577215664901532860606512090082402431042159335m;

        public static decimal Phi { get; } = (decimal)((1.0 * Math.Sqrt(5)) / 2.0);
    }
}
